# Azure V710 A-Card Workflow Issues

Run date: 2026-06-09
Host: `azure-v710`
Workflow: `remote-all-a` from `make/7-workflow.mk`, completed through redirected step logs under `tmp/logs/`.

| Issue | Affected step/app | Symptom | Solution | Verification |
|---|---|---|---|---|
| Existing remote dataset job was already running before the redirected workflow wrapper could proceed. | `remote-datasets` | A remote `make datasets` process was active on `azure-v710`, progressing through RTNN and RayDB dataset downloads/extraction. Starting another full workflow would have raced on `data/`. | Waited for the active dataset target to finish, then ran build, benchmark, and fetch phases separately with output redirected to `tmp/logs/`. | Dataset target exited; subsequent `remote-build-a` completed with exit 0. |
| Local background wrapper did not reliably write an exit marker for short SSH/make control commands. | Initial `remote-all-a` and `remote-build-a` attempts | Partial temp logs existed, but the wrapper process was gone and no `.exit` file was written. | Switched to synchronous `timeout ... > tmp/log 2>&1` invocations for build/run/fetch phases, keeping chat output bounded while preserving exit codes in log sidecar files. | `remote-build-a` exited 0 in `tmp/logs/remote-build-a-azure-v710-20260608-235452.log`. |
| A-RT-DBSCAN embedded an uppercase HIPRT source path. | `run-a-rtdbscan` | Runtime HIPRT kernel compilation failed with missing `/home/ubuntu/RT-Benchmarks/deps/HIPRT/src/hiprt/impl/BvhBuilderKernels.h`; actual path is `deps/hiprt/src`. | Changed `A_RTDBSCAN_HIPRT_PATH` from `${RTBM_DEPS_DIR}/HIPRT/src` to `${RTBM_DEPS_DIR}/hiprt/src`; patched the remote after `remote-update`, removed the stale build dir, rebuilt, and reran. | Direct remote `make build-a-rtdbscan run-a-rtdbscan` exited 0; all three A-RT-DBSCAN datasets completed and `artdbscan_result.csv` was fetched. |
| A-RT-BarnesHut embedded the same uppercase HIPRT source path. | `run-a-rtbarneshut` | All BarnesHut datasets failed with missing `/deps/HIPRT/src/hiprt/impl/BvhBuilderKernels.h`. | Changed `A_RTBARNESHUT_HIPRT_PATH` from `${RTBM_DEPS_DIR}/HIPRT/src` to `${RTBM_DEPS_DIR}/hiprt/src`; patched the remote, removed the stale build dir, rebuilt, and reran from BarnesHut onward. | Tail run exited 0; A-RT-BarnesHut, A-Mochi-DCD, and RayTracingInVulkan completed successfully. |
| A-Mochi-DCD and A-X-HD had the same incorrect uppercase CMake default, although their current runs were protected by exported `HIPRT_PATH` or had not failed yet. | A-Mochi-DCD, A-X-HD | Latent portability failure: rebuilding/running without the environment override would point HIPRT runtime source lookup at `deps/HIPRT/src`. | Changed CMake defaults to `${RTBM_DEPS_DIR}/hiprt/src` for both apps and forced affected remote rebuilds where needed. | A-Mochi-DCD completed successfully after rebuild; A-X-HD had already completed and its fetched CSV is present. |
| A-RTNN exceeded V710 memory on one large KITTI dataset. | `run-a-rtnn`, `KITTI0071` | `hiprtCreateGeometry` failed with Orochi out-of-memory on the 25 GB V710; the benchmark script recorded the OOM and continued. | No code change made because this is a hardware capacity limit for that input size on V710; retained the script's existing behavior of recording OOM and continuing. | `run-a-rtnn` did not fail the workflow; `expr/results/azure-v710/A-RTNN/artnn_result.csv` was fetched and includes the recorded run outcomes. |

Fetched result CSVs are under `expr/results/azure-v710/` for A-RayJoin, A-LibRTS, A-X-HD, A-RTTC, A-RTNN, A-RT-DBSCAN, A-RayDB, A-RTOD, A-RTSpMSpM, A-RT-BarnesHut, A-Mochi-DCD, and RayTracingInVulkan.
