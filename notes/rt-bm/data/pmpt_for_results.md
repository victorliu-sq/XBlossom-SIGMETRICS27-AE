[//]: # (RayJoin)
move on to RayJoin to seprate out bvh construction phase and ray-compute phase and get ray throughput.
Only consider time used by reder time or optix launch time. update source code, bash scripts, and python scipts.

Please commit and push the changes for `RayJoin` and in RT-benchmarks related to that submodule.
Then run the benchmarks to get reuslt back for this application only in aws-t4 only on all datasets and then let's check whether it works now.

[//]: # (X-HD)
move on to X-HD to seprate out bvh construction phase and ray-compute phase and get ray throughput in the same way as RayJoin.
Only consider time used by reder time or optix launch time. update source code, bash scripts, and python scipts.

Please commit and push the changes for `X-HD` and in RT-benchmarks related to that submodule.
Then run the benchmarks to get reuslt back for this application only in aws-t4 only on all datasets and then let's check whether it works now.

[//]: # (LibRTS)
move on to LibRTS to seprate out bvh construction phase and ray-compute phase and get ray throughput in the same way as RayJoin.
Only consider time used by reder time or optix launch time. update source code, bash scripts, and python scipts.

Please commit and push the changes for `RTSpatial and LibRTS` and in RT-benchmarks related to that submodule.
Then run the benchmarks to get reuslt back for this application only in aws-t4 only on all datasets and then let's check whether it works now.

[//]: # (RTTC)
move on to RTTC to seprate out bvh construction phase and ray-compute phase and get ray throughput in the same way as RayJoin.
Only consider time used by reder time or optix launch time. update source code, bash scripts, and python scipts.

Please commit and push the changes for `RTTC` and in RT-benchmarks related to that submodule.
Then run the benchmarks to get reuslt back for this application only in aws-t4 only on all datasets and then let's check whether it works now.

[//]: # (RTNN)
move on to RTNN to seprate out bvh construction phase and ray-compute phase and get ray throughput in the same way as RayJoin.
Only consider time used by reder time or optix launch time. update source code, bash scripts, and python scipts.

Please commit and push the changes for `RTNN` and in RT-benchmarks related to that submodule.
Then run the benchmarks to get reuslt back for this application only in aws-t4 only on all datasets and then let's check whether it works now.

[//]: # (RT-DBSCAN)
move on to RT-DBSCAN to seprate out bvh construction phase and ray-compute phase and get ray throughput in the same way as RayJoin.
Only consider time used by reder time or optix launch time. update source code, bash scripts, and python scipts.

Please commit and push the changes for `RT-DBSCAN` and in RT-benchmarks related to that submodule.
Then run the benchmarks to get reuslt back for this application only in aws-t4 only on all datasets and then let's check whether it works now.

[//]: # (RTOD)
move on to RTOD to seprate out bvh construction phase and ray-compute phase and get ray throughput in the same way as RayJoin.
Only consider time used by reder time or optix launch time. update source code, bash scripts, and python scipts.

Please commit and push the changes for `RTOD` and in RT-benchmarks related to that submodule.
Then run the benchmarks to get reuslt back for this application only in aws-t4 only on all datasets and then let's check whether it works now.

[//]: # (RTSpMSpM)
move on to RTSpMSpM to seprate out bvh construction phase and ray-compute phase and get ray throughput in the same way as RayJoin.
Only consider time used by reder time or optix launch time. update source code, bash scripts, and python scipts.

Please commit and push the changes for `RTSpMSpM` and in RT-benchmarks related to that submodule.
Then run the benchmarks to get reuslt back for this application only in aws-t4 only on all datasets and then let's check whether it works now.

[//]: # (RT-BarnesHut)
move on to RT-BarnesHut to seprate out bvh construction phase and ray-compute phase and get ray throughput in the same way as RayJoin.
Only consider time used by reder time or optix launch time. update source code, bash scripts, and python scipts.

Please commit and push the changes for `RT-BarnesHut` and in RT-benchmarks related to that submodule.
Then run the benchmarks to get reuslt back for this application only in aws-t4 only on all datasets and then let's check whether it works now.

[//]: # (Mochi-DCD)
move on to Mochi-DCD to seprate out bvh construction phase and ray-compute phase and get ray throughput in the same way as RayJoin.
Only consider time used by reder time or optix launch time. update source code, bash scripts, and python scipts.

Please commit and push the changes for `Mochi-DCD` and in RT-benchmarks related to that submodule.
Then run the benchmarks to get reuslt back for this application only in aws-t4 only on all datasets and then let's check whether it works now.

[//]: # (RayDB)
move on to RayDB to seprate out bvh construction phase and ray-compute phase and get ray throughput in the same way as RayJoin.
Only consider time used by reder time or optix launch time. update source code, bash scripts, and python scipts.

Please commit and push the changes for `RayDB` and in RT-benchmarks related to that submodule.
Then run the benchmarks to get reuslt back for this application only in aws-t4 only on all datasets and then let's check whether it works now.
